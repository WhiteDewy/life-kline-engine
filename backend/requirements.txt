# Backend runtime dependencies
# 与 pyproject.toml [project].dependencies 保持同步
# 安装方式一：pip install -e .  （推荐，从 pyproject.toml 读取）
# 安装方式二：pip install -r backend/requirements.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
anyio>=4.0.0
httpx>=0.27.0
pydantic>=2.5.0
numpy>=1.24
pyswisseph>=2.10.3
typing_extensions>=4.8.0
# 星灵咨询短期状态外置（可选；未安装时降级为进程内存，仅开发可用）
redis>=5.0.0
