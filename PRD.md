# 星灵花园 PRD

版本：v3.0  
更新日期：2026-07-31  
产品阶段：核心对话重构期  
文档职责：产品与实现的唯一需求基线

相关文档：

- [占星 AI Agent 架构](docs/agent-architecture.md)
- [工程规范](docs/ENGINEERING_STANDARD.md)
- [API 参考](docs/api-reference.md)
- [占星知识基线](docs/knowledge-base.md)

## 0. 文档规则

本文只描述当前产品决策、实现状态和可验收需求，不记录历史讨论。

状态标记：

- `DONE`：当前主链已实现并有测试。
- `PARTIAL`：已有代码，但体验或数据闭环不完整。
- `NEXT`：下一阶段必须实现。
- `LATER`：已确认方向，暂不进入当前里程碑。
- `DEPRECATED`：兼容存在，不再继续开发。

涉及产品形态、主 API、Agent contract、占星规则或数据模型的改动，必须同步更新本文。

## 1. 产品定义

### 1.1 一句话

星灵花园是一个以个人星盘为事实依据、由 10 颗行星星灵陪用户理解自己并持续成长的垂直占星 AI Agent。

### 1.2 用户价值

用户不需要懂占星术语。用户只需要把现实中的困惑说出来，产品应当：

1. 听懂用户此刻真正想问什么。
2. 从用户自己的星盘和时间背景中找到相关结构。
3. 用合适的星灵视角解释，而不是甩一份报告。
4. 允许用户确认、修正或否定解释。
5. 把用户确认过的认识变成可回访的成长线索。

### 1.3 产品边界

产品是占星咨询与自我探索工具，不是：

- 命运预测器或绝对答案机器。
- 心理治疗、医疗诊断、投资顾问或法律顾问。
- 用统一安慰话术包装的通用聊天机器人。
- 让用户依赖角色、恐惧未来或持续付费消灾的产品。

### 1.4 唯一产品形态

```text
Garden Keeper（统一入口与主持）
  + 10 颗行星星灵（用户的 10 个内在部分）
  + 12 个人生领域探索模块
  + 本命/法达/行运/月返等占星工具
  + 用户确认式记忆与成长系统
```

不再对外提供 12 星座人格角色。“星座”是行星表达风格和计算知识，不是独立聊天角色。
“星语者”“引擎占星师”“AI 占星师”不再作为并列产品身份。

## 2. 目标用户与核心任务

### 2.1 核心用户

- 20-40 岁，处于关系、事业、自我认同或人生阶段困惑中。
- 对占星有兴趣但未必专业，希望被解释清楚而不是被术语压倒。
- 需要即时回应，也愿意在信任建立后持续记录和复盘。

### 2.2 核心任务

| 用户任务 | 典型表达 | 产品应提供 |
|---|---|---|
| 被理解 | “最近很累，但我也说不清为什么” | 先承接，再澄清，不急着读盘 |
| 认识模式 | “我为什么总在关系里忍着？” | 本命结构 + 现实经历交叉验证 |
| 理解决策 | “我要不要离职？” | 价值、能力、风险与阶段视角，不替用户决定 |
| 理解时间 | “这种状态什么时候会过去？” | 法达/行运/月返背景 + 不确定性说明 |
| 情绪陪伴 | “今天只想找个人说说话” | 陪伴模式，不强行进入完整咨询流程 |
| 持续成长 | “上次说的边界问题我试了” | 调出已确认行动，比较变化并记录 |

## 3. 核心体验

### 3.1 主循环

```text
用户表达现实困惑
  -> Agent 理解意图与情绪
  -> 选择陪伴/快问/深度咨询/领域探索/议会
  -> 调用占星与记忆工具
  -> 星灵回应并给出可核对证据
  -> 用户确认、部分确认、否定或修正
  -> 形成洞察与一个小行动
  -> 后续回访变化
```

### 3.2 对话模式

| 模式 | 何时使用 | 回复目标 |
|---|---|---|
| 陪伴 | 用户想倾诉、信息不足、情绪较强 | 听懂与陪伴，最多问一个澄清问题 |
| 快问 | 问题清晰、只需一个结构解释 | 直接回答 + 证据 + 边界 |
| 深度咨询 | 重复模式、复杂矛盾、用户愿意探索 | 结构、现实取证、验证、洞察、行动 |
| 领域探索 | 事业、关系、财富等系统问题 | 多结构和时间背景的领域分析 |
| 星灵议会 | 问题涉及多个内在冲突 | 2-4 颗相关星灵给不同视角，Keeper 合成 |

深度咨询状态机是工具，不是所有聊天的唯一入口。用户插话、纠正、换题或只想倾诉时，
Agent 必须能离开固定流程并在合适时返回。

### 3.3 单轮回复标准

一次正常回复优先包含：

1. `承接`：准确回应用户刚说的内容，不空泛复述。
2. `理解`：指出当前问题的可能核心，低置信度时先澄清。
3. `占星解释`：只引用工具返回的相关事实。
4. `边界`：说明这是倾向、背景或待验证假设。
5. `下一步`：最多一个问题，或一个可执行的小行动。

回复不强制每次展示全部五段；情绪优先于报告完整度。

## 4. 角色体系

### 4.1 Garden Keeper

Keeper 是统一 Agent 身份，负责：

- 理解用户意图和选择模式。
- 决定由哪颗星灵回应，或主持星灵议会。
- 管理共享记忆、上下文和安全边界。
- 在用户不知道找谁时提供稳定入口。

Keeper 不是第 11 颗星灵，不拥有独立占星含义。

### 4.2 10 颗行星星灵

| 星灵 | 核心部分 | 典型议题 | 声音要求 |
|---|---|---|---|
| 太阳 | 自我定义与意志 | 我是谁、方向、被看见 | 清晰、坚定，不替用户下定义 |
| 月亮 | 情绪与安全 | 感受、习惯、照料、家庭 | 温和、具体，不滥用安慰 |
| 水星 | 思考与表达 | 沟通、学习、选择、信息 | 灵活、清楚，帮助拆解 |
| 金星 | 价值与关系 | 喜爱、自我价值、亲密、审美 | 有分寸、重视双方价值 |
| 火星 | 欲望与行动 | 愤怒、边界、竞争、执行 | 直接、有力量，不煽动冲动 |
| 木星 | 信念与扩展 | 意义、机会、远景、教育 | 开阔但不空泛乐观 |
| 土星 | 现实与责任 | 压力、限制、长期建设 | 稳定、诚实，不羞辱用户 |
| 天王星 | 自由与改变 | 独特性、突破、突变 | 新鲜、清醒，不为反叛而反叛 |
| 海王星 | 想象与边界 | 灵感、投射、共情、逃避 | 柔软但保持现实锚点 |
| 冥王星 | 权力与转化 | 控制、失去、阴影、重建 | 深入但不越界，不擅自创伤诊断 |

角色差异体现在提问角度、节奏和语言，不得改变星盘事实。

### 4.3 星灵选择

- 用户明确选择：尊重用户选择。
- 今日入口：用行运/法达激活推荐，并解释推荐理由。
- 自由提问：Keeper 根据问题、星盘证据和用户偏好推荐 1 颗主星灵。
- 内在冲突：调用议会，最多 4 颗星灵，避免角色噪音。

## 5. 占星专业标准

### 5.1 双轨原则

| 轨道 | 技术 | 回答什么 |
|---|---|---|
| 古典 | 七曜、传统守护、尊贵、接纳、飞星、法达 | 条件、资源、结构、阶段背景 |
| 现代 | 十行星、心理原型、相位模式、外行星 | 感受、动机、内在模式与发展可能 |

输出必须标识解释属于古典、现代或交叉验证，不得把两个体系混成一套无来源评分。

### 5.2 事实优先级

```text
用户现实信息
  + 本命宫位/宫主星
  + 行星落座落宫
  + 尊贵与偶然状态
  + 相位/接纳/飞星
  + 时间法
  -> 多个待验证解释
```

用户现实信息不是用来“推翻星盘”，而是用来选择正确取象；用户否定必须被记录为反证，
不得反复换话术证明系统永远正确。

### 5.3 外行星边界

天王、海王、冥王只进入现代心理和世代解释。它们不参与古典本质尊贵、传统宫主星、
接纳和法达计算。当前 `constants.py` 与 `planet_character_engine.py` 仍混入现代外行星尊贵，
状态为 `NEXT` 技术债，修复前不得对用户宣称这些分数属于古典体系。

### 5.4 时间判断

- 时间法提供阶段、窗口和触发背景，不输出“必然在某天发生”。
- 本命盘不能独立回答具体时间问题。
- 工具缺数据或出生时间不可靠时，必须明确降低置信度。
- 不把一次行运直接解释为事故、死亡、疾病、结婚或发财。

## 6. 12 人生领域

| 领域 key | 用户问题 | 主要结构 |
|---|---|---|
| `personal` | 我是什么样的人？ | 1 宫、命主星、日月、主导结构 |
| `appearance` | 别人如何感受我？ | 上升、1 宫、命主星 |
| `career` | 我的事业方向是什么？ | 10 宫、10 宫主、MC、相关行星 |
| `work_skill` | 我怎样工作最有效？ | 6 宫、6 宫主、水星、火星 |
| `education` | 我怎样学习与发展认知？ | 3/9 宫、水星、木星 |
| `finance` | 我的资源模式是什么？ | 2/8/5/11 宫及其宫主 |
| `romance` | 我如何恋爱与被吸引？ | 5/7 宫、金星、火星、月亮 |
| `marriage` | 长期关系如何建立？ | 7 宫、7 宫主、金星、土星 |
| `family` | 家庭经验如何影响我？ | 4/10 宫、月亮、太阳及文化语境 |
| `partnership` | 我怎样与人合作？ | 7/10/11 宫、利益与权责结构 |
| `children` | 我怎样创造与养育？ | 5 宫、5 宫主、月亮、木星 |
| `health` | 我如何观察身心节律？ | 1/6/8/12 宫；仅做非诊断性观察 |

领域模块不是 12 个独立机器人。它们是 Keeper 和星灵可调用的专业工具。

## 7. Agent 能力需求

详细技术契约见 `docs/agent-architecture.md`。

### 7.1 对话理解 `PARTIAL -> NEXT`

每轮至少识别：

- 用户行为：分享、提问、纠正、决定、命令、危机。
- 用户目标：理解、时间、选择、安慰、计划、复盘。
- 模式：陪伴、快问、咨询、领域、议会。
- 领域、情绪与强度、时间范围、关键实体。
- 状态动作：继续、确认、部分确认、否定、不确定、换题、暂停、结束。
- 置信度；低置信度必须澄清。

当前 v2 已有异步 LLM 分类 + 规则回退，但仍只输出扁平 `TurnIntent` 和结构话题，状态为 `PARTIAL`。

### 7.2 工具调用 `PARTIAL -> NEXT`

必须提供统一工具：本命结构、领域解读、时间背景、记忆读取、议会视角、确认洞察写入。

当前 `ChartReader`、领域引擎、法达、行运、Council 均存在，但没有统一 tool contract，状态为 `PARTIAL`。

### 7.3 证据约束 `PARTIAL`

- 每条事实带 `evidence_id`、来源、事实文本和适用范围。
- LLM 不能选择或生成星盘事实。
- 工具失败时说暂时无法读取，不补造。
- 用户侧技术证据默认折叠，按需展开。

当前 `SpiritDossier` 已实现基础 evidence 模型，跨工具统一尚未完成。

### 7.4 Provider `PARTIAL`

- Provider 无关，支持非流式、流式和结构化输出。
- 意图分类必须异步、超时、可降级。
- 记录模型、token、延迟、失败与降级。
- Prompt、知识和 Agent contract 独立版本化。

当前 `LLMClient` 支持多 Provider 与流式，但职责过多、结构化输出与可观测性不足。

## 8. 上下文、记忆与成长

### 8.1 四层记忆

| 层 | 内容 | 当前状态 |
|---|---|---|
| Working | 当前状态机、最近消息 | `DONE` Redis + 7 天 TTL |
| Conversation | 会话消息与恢复 | `DONE` v2 消息落库与页面恢复 |
| Semantic | 用户确认的模式、偏好、边界 | `PARTIAL` 仅确认洞察 |
| Growth | 行动、回访与前后变化 | `NEXT` |

### 8.2 写入规则

- 模型假设默认是 `pending`。
- 用户可选择确认、部分确认、否定、不确定和编辑。
- 只有确认/部分确认内容可进入长期语义记忆。
- 否定是重要信息，后续不得重复推销同一解释。
- 用户可查看、编辑和彻底删除消息、日记、记忆与成长数据。

### 8.3 成长不是活跃度

聊天次数、连续签到、亲密度和付费次数不是成长。

成长对象：

```text
主题 -> 用户原话 -> 已确认模式 -> 小行动 -> 回访日期
     -> 做了什么 -> 感受/结果变化 -> 用户确认的新认识
```

产品应在合适时间问“上次那个行动后来怎么样”，而不是自动宣布用户变勇敢了。

## 9. RAG 策略

结论：当前不先建设通用向量 RAG。

| 数据 | 方案 |
|---|---|
| 星盘事实 | 确定性工具查询，禁止 RAG |
| 占星规则 | 结构化知识/AKG 精确检索 |
| 用户记忆 | 按用户、主题、确认状态、时间做混合检索 |
| 占星文献 | 版本化、流派标注后可做 hybrid RAG |
| 案例 | 脱敏、授权、人工审核后仅用于取象候选 |

RAG 不能解决意图识别、上下文管理、状态推进或“人情味”。只有在知识覆盖评测证明需要时，
才进入 BM25 + vector + metadata filter + rerank 的实现。

## 10. 安全与伦理

### 10.1 强制护栏

- 自伤/自杀信号在所有占星和 LLM 调用前检测。
- 命中危机时立即脱离星灵角色，显示完整求助信息。
- 中国大陆全国统一心理援助热线使用 `12356`；紧急危险提示 `120/110`。
- 禁止宿命断言、恐惧营销、依赖诱导、医疗诊断、具体投资和法律指令。
- Prompt 护栏之外必须有后端输入/输出护栏。

危机前置检测和前端资源展示状态为 `DONE`；输出后处理、事件审计和地区化资源为 `NEXT`。

### 10.2 隐私

- 出生信息、经纬度、对话和日记按敏感数据处理。
- 进入 LLM 的上下文只包含必要星盘结论，不包含姓名、手机号和原始生日。
- 日志不得记录对话原文、验证码和完整出生信息。
- 用户必须有导出和一键删除路径。

## 11. 前端体验需求

### 11.1 聊天页

- 首屏显示星灵身份，但不展示整张“结构地图”压迫用户。
- 打字发送、快捷动作、插话和换题行为一致。
- 回复流式显示；失败不能留下空白气泡或半句话。
- 刷新和返回页面可恢复当前会话。
- 错误、额度和降级状态可见且有下一步。
- 危机资源保留换行和完整号码。
- 明示“星盘事实 + AI 表达 + 自我探索参考”，不宣称非 AI。

当前会话恢复、错误提示、合法历史角色和危机文案为 `DONE`；首 token 垫场、断连取消和
可访问性专项验收为 `NEXT`。

### 11.2 信息架构

一级入口只保留：

- 今日：今日星灵、每日一问、当前阶段。
- 对话：Keeper 最近会话、10 星灵、议会。
- 探索：12 领域、本命、时间工具。
- 成长：洞察、行动、回访、日记。
- 我的：出生档案、隐私、订阅。

## 12. 商业化原则

商业化必须围绕“更完整的理解与持续跟踪”，不能按用户情绪脆弱程度收费。

建议分层：

- 免费：建盘、今日入口、有限快问、首个深度咨询闭环。
- 会员：更多深度咨询、领域探索、长期记忆与回访、议会。
- 单次产品：年度/关系/事业等有明确交付物的专题咨询。

现有星币、VIP 和前端本地支付模拟口径不一致，状态为 `NEXT`；在真实支付接入前，
不得把 localStorage 解锁当作正式权限。

## 13. 数据指标

### 13.1 北极星指标

每周完成一次“用户确认洞察 -> 行动/回访”的有效用户数。

### 13.2 体验指标

- 首轮问题理解正确率。
- 话题路由准确率与用户纠正率。
- 用户反馈“被理解/说清楚了”的比例。
- 回复重复率、空回复率和事实幻觉率。
- 洞察确认/部分确认/否定分布。
- 行动创建率与 7/30 日回访率。
- 首 token P50/P95、整轮延迟、降级率、单轮成本。

不能只看 DAU、消息数和付费率。

## 14. 当前实现基线

### 14.1 当前主链 `DONE/PARTIAL`

```text
frontend /chat/:planet
  -> /api/v2/spirit-consultations/*
  -> SpiritConsultationService
  -> spirit_consultation state machine + dossier
  -> LLM stream / deterministic fallback
  -> Redis state + SQLite messages/insights
```

本轮已同步实现：

- 修复小写枚举值导致 LLM 意图和快捷动作失效。
- 语义意图分类移到异步服务层，规则作为降级。
- 修复 `spirit` 历史角色直接发送 Provider 的协议错误。
- 服务端持久化标准 `user/assistant` 消息并用于上下文。
- 页面刷新恢复 7 天内未结束会话。
- 前端显示真实错误、额度和完整危机资源。
- 全国统一心理援助热线更新为 `12356`。
- 星灵 Prompt 增加 voice spec、先回应用户原话和禁止重复。

### 14.2 兼容链路 `DEPRECATED`

| 能力 | 路由/模块 | 处理 |
|---|---|---|
| 12 星座角色对话 | `/api/characters/*` | 停止新增，迁移后删除 |
| V1 星灵对话 | `/api/spirit-chat/*` | 兼容期 |
| 旧五阶段对话 | `/api/spirit-chat-v2/*` | 兼容期 |
| 星语者咨询 | `/api/garden/consultation/*` | 合并到领域模式 |
| 文件态 ChatState | `llm_client.ChatStateStore` | 删除 |
| 旧 Memory/Growth 多轨 | `memory.py`、`growth/*` | 数据迁移后重建 |

## 15. 路线图与验收

### M0 主链止血 `DONE`

- 意图值、角色协议、消息持久化、恢复、危机资源修复。
- 真实场景回归测试覆盖。

### M1 Agent Contract 与评测 `NEXT`

- 实现结构化 `DialogueUnderstanding` contract。
- 建立 100 条人工标注黄金集，覆盖表达变体、纠正、插话和安全。
- 指标：意图/领域宏平均 F1 >= 0.85；危机召回测试 100%；星盘事实幻觉 0。

### M2 工具化占星内核 `NEXT`

- 封装 natal/domain/timing/memory/council 工具。
- 统一 evidence contract 和工具超时。
- 修正外行星与古典尊贵混用。
- 指标：工具选择准确率 >= 0.9；事实可追溯率 100%。

### M3 多模式对话 `NEXT`

- Keeper 自动选择陪伴、快问、咨询、领域和议会。
- 状态机支持插话、澄清、换题、返回和结束。
- 指标：重复回复率 < 3%；用户主动纠正率持续下降。

### M4 记忆与成长 `NEXT`

- 会话摘要、确认式语义记忆、行动与回访。
- 用户记忆管理与一键删除。
- 指标：确认洞察 7 日回访率、行动完成反馈率。

### M5 产品收敛 `LATER`

- 前端只暴露唯一产品形态。
- 退役旧角色、旧路由、旧状态和重复 Prompt。
- 在评测证明需要后决定是否引入 hybrid RAG。

## 16. 发布门槛

任何 Agent/Prompt/占星规则变更必须通过：

- 确定性引擎单元测试。
- API 契约与会话恢复集成测试。
- 对话黄金集评测。
- 危机、越界、宿命、工具失败安全测试。
- 前端桌面与移动端真实流式交互测试。
- 延迟、降级率、token 成本和负载测试。

未满足事实幻觉为 0、危机资源可见、用户数据可删除三项时，不得上线。
